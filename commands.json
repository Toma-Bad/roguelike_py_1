{
  "oneOf": [
    {
      "type": "object",
      "properties" : {
        "type" : { "const": "move"},
        "destination" : {
          "type" : "array",
          "items" : [
            {"type": "number"},
            {"type": "number"}
          ],
          "minItems": 2,
          "maxItems": 2
        }
      },
      "required": ["type", "destination"]
    },
    {
      "type": "object",
      "properties": {
        "type": {"const":  "attack"},
        "subtype" : {
          "enum" : ["melee", "ranged"]
        },
        "targetId": {"type":  "string"},
        "destination" : {
          "type" : "array",
          "items" : [
            {"type": "number"},
            {"type": "number"}
          ],
          "minItems": 2,
          "maxItems": 2
        }
      },
      "required": ["type"],
      "oneOf": [
        {"required": ["targetId"]},
        {"required": ["destination"]}
      ]
    },
    {
      "type":"object",
      "properties": {
        "type": {"const":  "interact"},
        "subtype": {
          "enum": ["talk", "trade", "get", "drop", "operate"]
        },
        "targetId": {"type":  "string"},
        "destination" : {
          "type" : "array",
          "items" : [
            {"type": "number"},
            {"type": "number"}
          ],
          "minItems": 2,
          "maxItems": 2
        }
      },
      "required": ["type"],
      "oneOf": [
        {"required": ["targetId"]},
        {"required": ["destination"]}
      ]
    }
  ]
}